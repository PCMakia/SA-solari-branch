# Install sibling MCP package first, then daemon deps:
#   pip install -e ../sleeper-mcp
#   pip install -r requirements.txt
#
# Or in one command from this directory:
#   pip install -e ../sleeper-mcp -r requirements.txt

pyautogui>=0.9.54
pygetwindow>=0.0.9
pyperclip>=1.8.2
